const fs = require('fs/promises');

const log_types = ['log', 'warn', 'error', 'critical', 'silly', 'annoying'];

/**
 * 
 * @param {*} message 
 * @param {*} type 
 */
function log(message, type) {

}